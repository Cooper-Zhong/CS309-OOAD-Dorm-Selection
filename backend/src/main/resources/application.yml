server:
  port: 8082
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  datasource:
    driver-class-name: org.postgresql.Driver
    # local database
    url: jdbc:postgresql://localhost:5432/cs309_ooad?serverTimezone=GMT%2b8
    # for aws
#    spring.datasource.url: jdbc:postgresql://cs309ooad.cjgxt58rjd8u.us-east-1.rds.amazonaws.com:1433/cs309ooad
#    spring.datasource.username: postgres
#    spring.datasource.password: ooad..sustech
#    spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    username: postgres
    password: 2333

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQL92Dialect
    show-sql: true
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: true


# Logger Config
logging:
  level:
    cs309_dorm_backend: debug

